<%= form_for(@list) do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% @list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Task %><br />
    <%= f.text_field :Task %>
  </div>
  <div class="field">
    <%= f.label :Comments %><br />
    <%= f.text_area :Comments %>
  </div>
  <div class="field">
    <%= f.label :Date_added %><br />
    <%= f.datetime_select :Date_added %>
  </div>
  <div class="field">
    <%= f.label :Finish_by %><br />
    <%= f.datetime_select :Finish_by %>
  </div>
<%  f.hidden_field(:user_id, :field, :value => current_user.id) %>
<input type="submit" value="Create Task" />
<% end %>
